{
    // Portable settings — no machine-specific absolute paths.
    // Works identically on Windows, macOS and Linux clones.
    "editor.formatOnSave": false,
    "typescript.tsdk": "node_modules/typescript/lib",
    "rust-analyzer.check.command": "check",
    "files.associations": {
        "*.ps1": "powershell"
    }
}
