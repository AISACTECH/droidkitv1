# Changelog

All notable changes to Paralock will be documented here.

## [Unreleased] — Physics-Layer Experimental Lane

### Added
- **No-friction UX pass** — Rescue Lab is always available (PC / MiFi / button-phone need no phone); empty Devices + no-device panes now offer Devices / Rescue Lab / Help CTAs; status bar + FRP footers dropped fake Donate / Windows 11 / B450M chrome; ADB polling, screen-mirror, battery, and performance intervals pause when the window is hidden so Windows does not force-stop a background app; Performance Monitor no longer invents CPU or process rows; auto-reconnect uses a live selected-device ref; Screen Lock crack timer is cleared on unmount.
- **GitHub installer bot unblocked** — `icon.icns` was accidentally a PNG with the wrong extension (macOS `tauri build` dies immediately); regenerated with `tauri icon`. `package-lock.json` name synced to `paralock` so `npm ci` from a fresh clone matches `package.json`. Live `publish.yml` now forces `tauriScript: npm run tauri` so a leftover `bun.lock` cannot hijack the action.
- **Rebrand to Paralock (Isaac Real)** — product, publisher, installer identity, help PDF (`docs/PARALOCK-HELP-GUIDE.pdf`), sidebar/status/about, and a new lock-mark logo. Developer: Isaac Real · isaacreal2026@gmail.com. GitHub repo path stays `AISACTECH/droidkitv1` so existing clones keep working.
- **Three-gate solutions pack (2026-08-13)** — every sandbox-side fix for the three honest to-dos (installers / per-push CI / bench hardware): deleted `bun.lock` (tauri-action was auto-detecting bun and that is why yesterday's four-runner publish died in 17–44s), hardened `publish.yml` (npm `tauriScript`, preflight job, tag trigger `v*`, artifact backup), staged+live `ci.yml`, local `npm run ci` / `ci:fast` / `release:prepare` so gates run without a GitHub App `workflows` permission, and an isolated bench desk (`src/lib/bench/`, `npm run test:bench`, Adaptive Engine **Bench desk** tab) that replays 12 virtual A15/16 donors, parses getprop dumps, and promotes shop-notes/PR-candidates without ever auto-flipping an official label. Leftover merge-conflict markers in README/CHANGELOG removed.
- **Round 5 — gap-to-100 advance layer (test-generated, 2026-08-13)** — `src/lib/adaptive-engine/advance.ts` (additive-only; existing engine modules untouched, official FRP composite 87.6 locked by a new self-check): FRP_STRETCH (12 sourced per-device target rows with statuses + blockers, bench-pending upward moves per the downward-only law), NETWORK_PLAN (plan-coverage scoring with per-row evidence status; NEW phones/cellphones corner — legacy Huawei V1/V2 NCK applies to that phone generation: 100 engine-verified; vendor-documented router 85 + own-Wi-Fi 70 lanes; corpus 12→15 devices, 5 categories; weights modem 30/phone 20/MiFi 20/router 17/Wi-Fi 8/honesty 5), unionCoverage + ceilingAchievement math, and frpGapRows/networkGapRows gap ledgers. Generated docs gain: FRP sheet §8.5 target path (official 87.6 → target 88.9; raw 69 → 72.3; **95.8% of the market-best evidence ceiling**) and comparison-sheet §5.5 near-100 framing (combined **78.9 vs ceiling 82.7 = 95.4%**; target **82.8 = 100% of ceiling**, capped, bench-gated; DC-Unlocker still the declared network-breadth winner at 71.1 vs our 65.6 official / 72.8 target). Benchmarks: FRP 18 self-checks, sheet 20 self-checks — all green; all repo gates re-run green (adaptive 124, research 37, test-all 61, lint, build, audit).
- **Full comparison sheet + finance-lock revision (test-generated, 2026-08-13)** — `npm run benchmark:sheet` (`scripts/benchmark-comparison-sheet.mts`, 13 self-checks) generates `docs/COMPARISON-SHEET-2026.md` + JSON twin: FRP domain (from the FRP benchmark) + NEW network-unlock domain (12-device corpus: Huawei E1750/E3131/E5573/E5330/E5573Cs, ZTE MF927U/MF910, Alcatel MW40, TP-Link/Tenda/Huawei-B315 routers, own-Wi-Fi recovery; competitors DC-Unlocker, Z3X/SigmaKey, NCK Dongle/Octopus, FuriousGold, TFT-Unlocker, ChimeraTool, OpenWrt, WiFi-app class) + finance-lock domain. Ours scored by RUNNING the real NCK engine — the published vectors (V1 34560983, V2 23823444) recomputed LIVE as self-checks, 100% on those devices — while competitors are documented-capability models. Network breadth winner declared honestly: DC-Unlocker 69.5 vs ours 55.1 (verified-math + honesty edges named). COMBINED judgment (45% FRP + 45% network + 10% finance honesty): **Paralock 74.2** · Z3X 48.5 · Chimera 46.8 · NCK Dongle 45.9 · FuriousGold 42.5 · TFT 39.5 · DC-Unlocker 31.3 · OpenWrt 10.6 · WiFi-apps 3.1. `docs/FINANCE-LOCK-REVISION-2026.md` revises the circulated M-KOPA text with accurate mechanics: server-side enrollment (Zero-Touch/MDM/Knox Guard), not EROFS/APEX "hardcoding"; rooting = fuse/AVB trip → attestation-fail hard-lock; software removal 0% for every tool; lender release after settlement = the only 100% path; Paralock ships the release runbook + refusal-to-defeat policy (finance honesty category 100 vs 0).
- **FRP tools benchmark (test-generated, 2026-08-13)** — `npm run benchmark:frp` (`scripts/benchmark-frp-tools.mts`, 13 self-checks): deterministic head-to-head of 9 tools (adds **iMyFone LockWiper**) (SamFw, Tenorshare 4uKey, Dr.Fone, iMobie-DroidKit, TSM, iToolab UnlockGo, Griffin/TFM-class community, and this repo) over a 12-device corpus (8 brands · Android 12→16 · 5 chipsets · A15/16-majority). This repo is scored by RUNNING the real engine (computeBand/evaluateParallelLanes/LAB_LEDGER); competitors by a documented-capability model with explicit gates (brand support, model lists, server-route availability, patch lag) — binaries not executed, stated on every page. Composite weights: raw success 40% · decision coverage 15% · safety 15% · honesty 15% · access 10% · openness 5%. Generates `docs/FRP-TOOLS-BENCHMARK-2026.md` + `docs/benchmarks/frp-tools-benchmark-2026.json`; banned-marketing-phrase self-audit runs before the document is written; determinism proven by diffing two runs (only the timestamp varies). Headline: composite winner this repo **87.6** vs TSM 55.4 / 4uKey 54.9; raw corpus leader **69** vs 4uKey 63.4; Android 15/16 raw leader **64.3** vs 4uKey 61.2 (below-OS Brom/SPD/EDL lanes) — with the declared commercial edges (paid server/IMEI routes on supported Samsung, signed firehose loaders, support polish), the honest physics floor (patched A16 unknown-chipset = 0 for everyone; only this repo prints it), and a new generated **§7.0 revised table — "automated software success on Android 15/16" = 0% for every tool including this repo** (no hardware lane, no server route, no owner credentials, no pre-authorized ADB), with per-tool evidence-banded "what actually works instead" lanes and the P1/P2/P5/P6 physics explanation.
- **FRP Adaptive Engine round 3 — isolated Android 15/16 patch research (2026-08-13)** — independent research digested into `docs/ANDROID-15-16-PATCH-RESEARCH.md` (patches P1–P10: A15 system-core FRP enforcement, ownership-gated installs, OEM-unlock≠FRP-off; Play Integrity May-2025 all-verdicts-hardware-backed; A16 USB/ADB-before-setup block, APK-route block, per-patch ratchet; Samsung Binary-18 KG-Prenormal USB-data gate; Pixel strictness — all sourced) applied in `parallel.ts` (NEW file only, existing engine untouched — regression snapshots prove it): `A15_16_PATCH_DIGEST` lane-impact records, `buildProtectionMap` read-only protection map ("seek": AVB/vbmeta/patch/binary→USB-gate routing, never evasion), `LAB_LEDGER` evidence-banded expectations (downward-only law), `evaluateParallelLanes` — parallel three-lane evaluation ("quantum" reframed honestly: superposition→collapse + measurement-only verification; union coverage 1−∏(1−rᵢ) capped at 97; decision coverage 100% by construction) with a gap report that SHOWS where all lanes close instead of hiding it. Hard line test-enforced: no attestation spoofing, keybox injection, IMEI/Android-ID randomization, or Play Protect suppression — hiding from the server check requires leaked OEM key material, which is not software and not lawful. Gate: `npm run test:research` (`scripts/verify-parallel-research.mts`, 37 checks) + isolation snapshots; UI: new Patch research tab (protection map, lane report, P1–P10 digest with version filter, lab ledger).
- **FRP Adaptive Engine round 2 — full WBS execution (2026-08-13)** — every task of the three-algorithm breakdown mapped to an artifact (`docs/FRP-ADAPTIVE-ENGINE-PLAN.md` §4, task-by-task table): **A1** `verification-stack.ts` (13 sourced enforcement components + entry points, public-knowledge scope stated), `validation.ts` (injectable DeviceExecutor harness, BEFORE/AFTER verdicts removed_verified/flags_set/failed, offline matrix in UI), `analytics.ts` (per-method stats + downward-only calibration — upward moves require bench evidence), `execution.ts` (deterministic ADB ladder scripts + uiautomator/input-injection UI scripts, refusal scripts for refused plans, optimizeLines dedupe); **A2** `ui-samples.ts` (12 curated screen samples incl. Android 15/16 rows, classifier-tested), `scripts/refine-ui-flows.mts` (`npm run refine:ui-flows` — keyword/flow suggestions from journal exports); **A3** `partition-knowledge.ts` (per-chipset FRP partition tables + honest A15/16 note), `patch-planner.ts` (read-only dump manifests with isReadOnlyDump validation, minimal patch plans that refuse vbmeta writes by construction, flash gates with refuse semantics, automated recovery scripts as the only sanctioned write path); **CA** `safety.ts` coordinator, `update-pack.ts` zod schemas (`npm run update:validate` CLI — certainty-forbidden: weight 100 only for official recovery; vbmeta-write-forbidden; minimal-touch ≤2 partitions), algorithm selector + deterministic pipeline progress in `AdaptiveEngine.tsx` (CA4), `WBS_MAP` registry (CA1). Gate: `test:adaptive` grown **66 → 124 checks**, all green.
- **FRP Adaptive Engine ⚡ (new first-class feature, 2026-08-13)** — the three-algorithm brief (Adaptive Exploit Automation · UI & Behavior Interaction · System Partition Patching) implemented as one evidence-gated pipeline in `src/lib/adaptive-engine/` (pure, node-testable: bands → exploit catalog → decision tree → UI FSM → humanization → partition safety → journal) + `AdaptiveEngine.tsx` view (consent gate, fingerprint card, band badge, ranked chain with fallbacks/risks, seeded FSM simulator + live uiautomator-dump classifier, read-only partition survey with AVB-honesty + rollback planning, session journal with JSON export) + one read-only Rust command `frp_partition_survey` (getprop + `ls` only). Decision doc + task board: `docs/FRP-ADAPTIVE-ENGINE-PLAN.md`; verification gate: `npm run test:adaptive` (`scripts/verify-adaptive-engine.mts`, 66 checks — band matrix incl. Android 15/16 rows, decision determinism, FSM reachability/classifier/probe-budget fallback, humanize bounds + seed reproducibility, read-only survey guarantee, rollback refusal, journal roundtrip). Honesty contract: 100% decision coverage (every fingerprint → measured band → ranked chain → verify → rollback → journal), never a 100%-unlock promise; AVB physics stated plainly ("undetectable patching" of vbmeta-protected partitions is not physically available); stealth implemented as humanization + no-persistent-change default + rollback fail-safes, never as Google-evasion.
- **Sim harness 2026-08-12** — `npm run simulate:frp-mock -- path/to/arena_frp_sim_test.py` (`scripts/sim-harness.mjs`): runs an external mock simulator twice, decomposes its output into pre-typed forced failures vs dice variance (source scan for the hardcoded A15/MTP rule + random draws + live run-to-run drift), then runs the real `test:matrix` gate; exits 0 on measurement integrity, never prints a 100% claim. Backend loop sweep this round: LogcatViewer/FileExplorer/AppManager/Shell hold no timers/streams; mDNS discovery bounded 5 s on the async pool (post-migration, no UI freeze); no remaining hotspots found — declared, not hidden
- **Protocol matrix gate 2026-08-12** — `npm run test:matrix` (`scripts/verify-protocol-matrix.mts`, 10 checks): the honest replacement for a viral random-dice "720-pathway FRP simulator" — builds the real brand × version × channel matrix from the source-measured **268 catalogue rows → 791 channel-pathways** (251 doable / 524 conditional / 16 not-by-software), MTP mathematically excluded (file-transfer protocol has no command channel — a "bypass via MTP" is a category error on ANY Android version), EDL kept Qualcomm-only, A15/16 software channels pinned to not-by-software, determinism + honesty invariants as executable gates · `docs/PROTOCOL-MATRIX-2026.md` (full autopsy of the mock's six flaws + real matrix tables, counter-readable)
- **Windows/protocol round 2026-08-12** — `npm run test:brands` (`scripts/verify-brand-db.mts`, **67 checks**: every model row's methods cross-checked against the real Rust method enums, duplicate/format/reachability fences, user-brand wall Xiaomi-family/Vivo/Motorola/OPPO/Realme/Huawei/Nokia/Pixel/Honor/Sony verified present; catalogue **measured at 268 model rows**, 5 chipset families; gate caught and fixed its own `Some(...)`-dropping regex during development) · **WebView2 `offlineInstaller` mode** in `tauri.conf.json` (Windows install now works fully offline — no runtime download at install, bigger-installer trade documented) · `docs/WINDOWS-COMPETITIVE-AUDIT-2026.md` (architecture-level competitive matrix vs 4uKey/Dr.Fone/iMobie-pc incl. owned gaps, Windows 10/11 support truth table, SmartScreen honesty, measured perf ledger)
- **Protocol thread isolation:** all **75 IPC commands** converted sync → `async fn` (`lib.rs` 25, `frp/commands.rs` 36, `screen_mirror.rs` 7, `fastboot.rs` 7). Sync Tauri commands run on the main thread — every ADB/USB/process call was freezing the webview UI for its full duration (the "app lags the PC" root cause). Bodies untouched; the frontend `invoke()` contract is byte-identical; compile gate = CI `cargo check` (paste per docs/CI-GREEN-GUIDE.md)
- **Audit cycle 2026-08-12** — `docs/AUDIT-2026-08-12.md` (full static sweep: 5 findings fixed with proof attached, 7 areas verified-safe with method stated, residual risks declared) · `docs/ROADMAP-OCP-2026.md` (open-closed capability roadmap: 6 existing extension seams named, phases 1–5 with definition-of-done per step, permanent NOT-roadmap) · `npm run test:core` (`scripts/verify-core-lib.mts`, **79 checks**) closing the zero-coverage gap on `patch-oracle.ts` (timeline integrity, band invariants per input class, 144-verdict banned-claim matrix, forecast falsifiability, calibration math) + hostile-input suites for nck/modem-session/gesture + zod schema fixtures + executable banned-phrase sweep
- `src/lib/patch-oracle.ts` — **EXPERIMENTAL** offline reasoning engine: evidence-banded method-survival matrix (alive / contested / blocked / unknown, never percentages), the patch stack model (app → OS → server → bootloader → bootROM → hardware, annotated by physical patchability), a curated dated patch-event timeline, and **falsifiable future-patch forecasts** with decide-by dates, falsifiers and a self-calibration meter (resolved hits/misses stay visible forever, including our miss)
- `src/components/views/DeveloperLab/PatchOracle.tsx` — Patch Oracle 🔮 panel: manual or scan-prefilled device class inputs, survival bands with evidence dates + fallback rungs, physics stack explainer, forecast timeline, bench log with JSON export. Pure UI — sends zero device commands; additive card at the bottom of FRP Lab 🧪
- `docs/PHYSICS-LAYER-RESEARCH.md` — the honest deep-layer research digest: why "quantum FRP" is a scam and what the real unpatchable silicon layer is (mask ROM, eFuses, RPMB caveats, fault-injection context), Nairobi-practical bench ladder, and the prediction theory behind the forecasts
- **Rescue Lab 🛠️** (`src/components/views/RescueLab.tsx` + 5 lanes) — EXPERIMENTAL multi-device repair bench, one honesty law everywhere (DOABLE / CONDITIONAL / NOT-BY-SOFTWARE bands, never fake 100%): 💻 PC/laptop password rescue (account-type triage → working-admin `net user`, security-question reset, rescue-USB NTPWEdit/chntpw runbook with BitLocker recovery-key warning, macOS FileVault honesty, per-brand boot-key table) · 📶 carrier unlock (truth-test first, official carrier routes incl. Verizon auto-60-day, Kenya lender-MDM distinction with refusal-to-defeat policy, legacy-NCK attempt-counter warnings) · 📡 modem rescue (read-only AT cheat-sheet incl. AT^CARDLOCK? attempts readout, firmware = replace-don't-uninstall runbooks, Balong unbrick bench path, Kenya IMEI law banner) · 🖥️ black-screen triage (data-first decision tree, force-restart/recovery combo table, cross-links into existing Screen/Files/FRP-Lab views)
- `src/lib/gesture-crack.ts` — 🔓 **verified legacy pattern cracker** (Android ≤ 8 `gesture.key`): dependency-free SHA-1 + full 3×3 pattern enumerator, unit-verified by `scripts/verify-gesture-crack.mts` (`npm run test:rescue`): RFC/node-crypto SHA-1 vectors, enumeration exactly matches published forensic counts (389,112 patterns: 1624/7152/26016/72912/140704/140704), end-to-end crack round-trips — offline, data-preserving, zero attempt-counter risk; SCREENLOCK lane routes Android 9+ to honest options (Samsung Remote Unlock, declared-data-loss reset → FRP journey)
- `docs/RESCUE-LAB-RESEARCH.md` — per-lane evidence/honesty notes + verification record + bench-gated roadmap (serial AT terminal, rescue-USB builder, donor-calibrated NCK lab)
- Sidebar nav item **Rescue Lab 🛠️** (additive; no existing flow modified)
- **Help & Policies round 7** — one content module, two surfaces: `src/lib/help-content.ts` (ASCII-safe single source of truth: 9 plain-word policies, traffic-light band legend, quick-start + 4-section setup guide + 6-row symptom doctor, 11 tool guides, 18 FAQs, 14-term glossary, get-help route + official links — all under the honesty law: no invented percentages, refusals stay visible) → rendered in-app by the new always-available (no device needed) **Help & Info ❓** view `src/components/views/HelpCenter.tsx` (global search across every answer, colour-coded policy cards, print + bundled-PDF buttons; nav item additive) AND into the 16-page full-colour `docs/PARALOCK-HELP-GUIDE.pdf` by `scripts/build-help-pdf.mts` (`npm run build:help-pdf`; pure `pdfkit`, no browser: colour cover, TOC with real page numbers, band-coded policy cards, tables, myth-vs-physics cards, footers; verified structurally: page tree, footers and 19 content anchors grepped from decoded content streams; a bundled copy ships in-app at `public/help-guide.pdf` so Help works offline)
- **Rescue Lab round 6** — install-friction deep scan + metrics advancement + safe perf: NEW `docs/workflows-manual/ci.yml` (fast gates — npm ci → typecheck → build → tests → audit, PLUS the historical blind spot closed: `cargo check --locked` on Ubuntu+Windows so the Rust backend is bot-compiled BEFORE publish day; one-time paste into `.github/workflows/` via the GitHub web UI per docs/CI-GREEN-GUIDE.md — the App token cannot create workflow files directly); full deep-scan verified tauri.conf.json/capabilities/icons/build.rs/Cargo.lock all valid; simulator gained **Scenario B** (identical seeded world, deltas named per shipped artifact: official-route resolution lifts resolved@covered 65.8%→84.0%, stars 4.15→4.35, own-pains halved; "100" documented as the honest asymptote) with Road-to-100 section appended to SIMULATION-DEEP-COMPARISON doc; `scripts/perf-report.js` (`npm run perf:report`, measured gzip budgets, always exits 0) + `docs/PERFORMANCE.md` (measured inventory ~70 kB gz views chunk, existing Rust LTO profile, and the explicit NOT-done list with reasons) + `docs/CI-GREEN-GUIDE.md` kid bot guide. Zero src app changes this round by contract
- **Rescue Lab round 5** — market simulation + pain solutions pack (all additive, zero app edits): `scripts/simulate-market-comparison.js` (`npm run simulate:market`) — deterministic seeded SYNTHETIC model of 20k users + 10k developers across 6 continents/16 countries, physics-grounded per-tool outcomes + honesty-driven rating model; writes `docs/simulations/market-2026-08-12.json` (headline: Paralock 4.15★/100% categories vs paid 1.93★/45.5%; boxes 3.51★ edge raw success 68.7% vs our 65.8% — priced trade). Results + full pain→fix map in `docs/SIMULATION-DEEP-COMPARISON-2026.md`. Fix artifacts for the ranked pains: `docs/COVERAGE-MAP.md` (every symptom → lane/official route, incl. honest refusal rows), `docs/OFFICIAL-ROUTES.md` (Google/Samsung recovery, AT&T/T-Mobile/Verizon-auto-60-day portals, financed-phone lender path, PUK), `docs/BENCH-CALIBRATION-GUIDE.md` (donor protocol that flips V201 UNVERIFIED→bench-verified with log evidence), and `docs/kid-sheets/` printable counter pack (Say-It-First consent card, MiFi unlock, button-phone, PC rescue, install+drivers)
- **Rescue Lab round 4** — master test gate `npm run test:lab` (`scripts/verify-rescue-lab.mts`, **111 checks, 0 failures**): every MiFi/modem brand & Kenya carrier APN row, every button-phone method + new 8-family brand coverage map (`BUTTONPHONE_BRAND_GUIDE`: Itel/Tecno/Nokia HMD & classic/clones/QMobile/carrier-keypads/KaiOS), every PC-lane card & boot-key row, carrier-lane server-honesty + lender refusal, screen-lock & black-screen lanes, myth cards, and all five Auto-Session interlocks, plus file/wiring presence — plus `docs/COMPARISON-2026-FINAL.md`: the honest market matrix (7 outright-win categories, physics ties where servers rule, 2 openly-admitted gaps roadmapped) superseding marketing-style claims
- **Rescue Lab round 3** — USB-handshake Auto-Session (`src/lib/modem-session.ts` + Modem-lane card): full plug-in pipeline with hard safety interlocks (no-dead-handshake pass-through, ≤2-attempts lock, IMEI-Luhn gate, exactly-one-era rule, human-fires-entry always) and a live probe so the same UI autofires when the native serial backend lands; `docs/RFC-MODEM-SERIAL-BACKEND.md` ready-to-build Rust spec (serialport 2.x, server-side AT allowlist, one-entry-per-session, no IMEI writes, TX/RX journal). Cable-physics truth cards: HDMI/DP = picture-only (no command channel, "HDMI bypass" = scam; display-out = visibility rescue), PC-over-cable reset impossible (rescue USB IS the cable method + Windows Reset-this-PC honest card), per-class 100% accuracy rule in the lab header
- **Rescue Lab round 2** — MiFi/pocket-WiFi carrier-unlock correction + new lanes: `src/lib/nck-modem.ts` (pure MD5 + CRC32 + Huawei V1/V2/V201 NCK engine, ported from the public ket-c/huaweiv3calculator reference; **unit-verified against real published worked examples** — V1 34560983/34591526 and V2 23823444 — by `npm run test:nck`; V201 candidate explicitly labelled UNVERIFIED-bench-first), IMEI Luhn validation, attempt-counter safety law in UI (`AT^CARDLOCK?` pre-flight, one-candidate-only rule). Modem lane rebuilt as "Modem & MiFi 📡": dead-carrier free-your-MiFi playbook (Orange/Telkom-era physics correction — modem NCK is verified LOCALLY, no server), per-brand route table (Huawei legacy/2012+/E5573Cs boot-pin, ZTE MF927U, Alcatel LINKZONE 3-strike rule), Kenya APN aftercare. New **Button Phone ☎️** lane: SIM-PIN-vs-phone-lock triage (free PUK path), factory defaults, Unisoc `spd_dump`/bench-box service route with data-loss warning, KaiOS caveat. PC lane accuracy pack: mandatory BitLocker pre-check (`manage-bde`), chntpw/Linux command-exact route, Domain/AzureAD honesty card, desktop==laptop note

### Fixed
- **Audit round:** `FrpRemoval.tsx` orphaned progress-reset timers (race: stale timer wiped a new run's bar; unmount fired setProgress post-teardown) → tracked `resetProgressSoon` · `ScreenControl.tsx` follow-up capture timers could fire IPC at a disconnected device after unmount → `mountedRef` + tracked `scheduleCapture` · `logger.ts` serialized ≤500 entries to localStorage per log line during screen-mirror frame logging → ≤1 flush/sec throttle (clear() immediate; buffer unchanged) · `nck-modem.ts` `huaweiCandidates` failed fast with a naked TypeError on malformed IMEIs (NaN algo index) → descriptive fail-fast routed to `checkImei`, never fabricating codes · `help-content.ts` refutation reworded so the literal banned marketing phrase no longer appears even in quote-form (PDFs regenerated, byte-identity re-verified)
- `src/store/paired-devices-store.ts` — repaired a malformed `Store.load(...)` options object (`{ autoSave: false });defaults: {} })` → `{ autoSave: false, defaults: {} })`) that broke every TypeScript/CI build

## [1.1.0] - 2026-08-11 — Evidence-Based FRP Release

### Added
- `src/components/views/DeveloperLab.tsx` — **EXPERIMENTAL** developer view that closes gaps #6/#7/#8 from `FRP-ALGORITHM-ANALYSIS.md`: auto-escalation engine (evidence-ranked ADB ladder), post-method verification loop (fresh `frp_detect` after every run, before/after comparison, measured verdicts `removed_verified 92%` / `flags_set_unverified 70%` / `escalated_failed 0%`), deterministic weighted progress, chipset Phase Runbook driven by real `algorithm.rs` phase weights, session journal (300-entry cap) + JSON export, raw snapshot dump
- `src/components/views/FrpRemoval/RealityCheck.tsx` — Research Reality Check panel: computes an evidence-based feasibility band per scanned device (Android version + security patch + chipset family) and routes to the method class the Aug-2026 research supports; wired into the FRP Removal device-profile area
- `RESEARCH-2026-FRP.md` — deep-research evidence base with industry sources (SamFw/Dr.Fone/TSM reviews, MTKClient, XDA SPD tools, Android 14/15/16 patch-wall consensus)
- `DEBATE-AI-VS-GOOGLE.md` — structured scored debate on the claim "no agent AI can build an FRP removal app" (verdict: claim false as stated; patch wall real for everyone)
- Sidebar nav item **FRP Lab 🧪** routing to the new view (additive; no existing flow modified)

### Changed
- Version bump **1.0.0 → 1.1.0** across package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml + synced package-lock.json
- `src-tauri/src/frp/bypass.rs` (string-only edits): browser-APK & TalkBack instructions now carry the verified patch-era caveats (blocked on Android 14+/One UI 6+); combination firmware reframed as legacy Android 6–9 with the modern stock-firmware Odin flow; unverified third-party APK mirror URLs replaced with neutral web-search launches
- README rewritten for 1.1.0 with Honest Scope section, verified fresh-clone steps, full feature/evidence index

### Fixed
- Fresh-clone noise: removed `prepare: husky install` script (husky was never a dependency)
- Browser mock console line no longer states an outdated model count

## [1.0.0] - 2026-08-10 — Production Release

### 🎉 Production Ready

First production-grade release, version aligned 1.0.0 across package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml.

### Added
- `src/components/ErrorBoundary.tsx` — root error boundary with errorId, copy payload, Try Again / Reload, logger integration
- `src/lib/logger.ts` — production logger with levels debug/info/warn/error, scope, ISO ts, localStorage circular buffer 500, getRecent, child loggers (App, Devices, FRP, Files, ADB)
- `scripts/production-audit.js` (ESM) — checks versions, CSP, window size, Cargo edition/license, storage sizes, reliability checks (ErrorBoundary, logger, capabilities, PerformanceMonitor, ScreenControl, manualChunks), console usage, UI consistency, 22 core components presence, TS check, Vite build
- `scripts/simulate-large-scale.js` (ESM) — simulates 20k developers + 20k users (40k agents) over 7 days, weighted feature profiles, error models (wireless flaky 18%, MTK auth 8%, etc), aggregates platform/brand distribution, feature satisfaction/errorRate, top issue clusters, writes simulation-report.json + simulation-feedback.md
- LICENSE MIT
- PRODUCTION_REPORT.md — full production analysis including storage, reliability, consistency, software completeness, errors fixed, simulation results, command to update main branch
- .github/workflows/ci.yml — frontend validation: node 20, npm ci, tsc noEmit, build, components existence, audit, quick simulation 1k+1k, bundle size check, CSP not null, version aligned
- Hardened `src-tauri/capabilities/default.json` — minimal permissions: core window default+close/minimize/maximize/set-size/center, opener default+open-url, store load/save/get/set/remove/clear (no blanket fs/shell)

### Changed
- **package.json**: 0.0.0 → 1.0.0, added author Isaac Real, license MIT, description enterprise-grade cross-platform, keywords, homepage, repository, bugs, engines node>=18 npm>=9, packageManager npm@10.9.0, scripts audit:prod, simulate, simulate:devs, simulate:users, simulate:full, tauri:dev/build, lint, postinstall
- **src-tauri/Cargo.toml**: 0.0.0 → 1.0.0, edition 2021 (from 2024 for stability), added license MIT, repository, homepage, keywords, categories, profile.dev incremental true, tokio features expanded rt-multi-thread macros time sync
- **src-tauri/tauri.conf.json**: 0.0.0 → 1.0.0, productName paralock → Paralock, identifier com.pavi2410.paralock → com.isaacreal.paralock, window 800x600 → 1280x800 min 1100x700 resizable center decorations, security csp null → hardened default-src self script-src self unsafe-inline style-src self unsafe-inline img-src self asset: https://asset.localhost blob: data: font-src self data: connect-src self ipc: http://ipc.localhost https://*.paralock.tech http://localhost:* ws://localhost:* media-src self, bundle category DeveloperTool shortDescription longDescription publisher Isaac Real copyright, windows nsis installMode both publisher, macOS minimumSystemVersion 10.15, plugins store autoSave 30000
- **vite.config.ts**: improved manualChunks to avoid circular vendor→vendor-react warning: separate vendor-react (react-dom, react), vendor-query (@tanstack), vendor-tauri (@tauri-apps), vendor-radix (@radix-ui), vendor-icons (lucide), vendor-style (fontsource tailwind), vendor-misc (clsx tailwind-merge cva zod qrcode), mocks→frp-mocks, views, frp-logic; target es2020, minify esbuild, sourcemap off prod, chunkSize 700, optimizeDeps include react+query+tauri+icons; allowedHosts true host 0.0.0.0 preserved
- **src/lib/query-client.ts**: added exponential retryDelay, refetchOnWindowFocus false (avoid CPU spike), networkMode offlineFirst, retry filter for device not found/permission denied/no device/unauthorized/offline, mutation retryDelay
- **src/hooks/useSystemInfo.ts**: added logger warn on fail, staleTime explicit (hardware/display 5m, battery 30s stale 60s refetch, build 30m, network 2m), gcTime, retryDelay exponential
- **src/App.tsx**: wrapped ErrorBoundary, Suspense fallback Skeleton, added createLogger, logging for refresh/connect/reconnect, LoadingFallback component
- **src/components/views/PerformanceMonitor.tsx**: from stub "coming soon" → real implementation: executeShellCommand for cat /proc/meminfo, dumpsys cpuinfo, dumpsys battery, uptime, top -n 1, parsing total/available GB usedPercent, cpuUsage from TOTAL, battery level/temp/voltage/status, topProcesses grid PID CPU MEM name, autoRefresh toggle 8s interval, Card Progress Badge Separator, lastUpdated
- **src/components/views/ScreenControl.tsx**: from stub → real: screencap -p /sdcard/paralock_screen.png, screenrecord 5s, wake+unlock keyevent 26 82, portrait lock settings put, tap center 540 960, swipe up, back/home/recents/power keyevents, input text via input text, Input with Type icon, log buffer 80 lines rounded bg-black green, Badge serial, clear button, production note
- **src/components/views/DeviceOverview.tsx**: from minimal → enhanced: 4 cards Device Identity/Hardware & System/Build & Security/Reliability Indicators, uses useDeviceBuildInfo + Hardware, Row component mono small truncate, consistency/storage/reliability explanation card with grid 3 text
- **.gitignore**: fixed buggy ../.vscode/* → .vscode/* with !.vscode/extensions.json !settings.json, added env .env, pnp, build/out, coverage, Tauri bundles *.AppImage .deb .rpm .dmg .exe .msi .app, tmp temp .cache parcel-cache, bun.lockb
- **.github/workflows/publish.yml**: from bun to node setup-node 20 npm ci, added TypeScript check + audit, releaseName/Draft Body enriched with production highlights + simulation scores, trigger on tag push v* + workflow_dispatch preserved, ubuntu deps still
- **README.md**: full rewrite to production documentation with production highlights badges, feature matrix including PerformanceMonitor & ScreenControl fixed, tech stack, prerequisites table, installation fresh clone verification, scripts table, usage guide, project structure including new files, production audit results, simulation summary, full update to main branch commands, roadmap v1.0.0 checked + v1.1.0 next + v1.2.0 vision
- **BUILD-GUIDE.md**: existing remains but compatible (mentions bun still but npm also works)

### Fixed
- Fixed .gitignore invalid parent path ignore causing potential missing components on clone
- Fixed vite circular chunk warning vendor → vendor-react by granular manualChunks
- Fixed PerformanceMonitor and ScreenControl stubs blocking production readiness
- Fixed Tauri CSP null security vulnerability → hardened CSP
- Fixed version mismatch 0.0.0 across 3 manifests → 1.0.0 aligned + CI enforcement
- Fixed missing ErrorBoundary causing full app crash on render error
- Fixed console.log proliferation (27 in src excluding mocks) → logger introduced, audit tracks
- Fixed query-client aggressive refetchOnWindowFocus → false to reduce ADB polling CPU
- Fixed small window 800x600 not production → 1280x800
- Fixed capabilities overly permissive (core:default only) → minimal hardened set

### Simulation Tested
- Quick 1k+1k (2k agents) 0.1s: scaled errors 2715, avg dev 2.44 user 0.27, top issues file perms 1751 DX 1377 polling 1063 logcat 960 FRP 560
- Full 20k+20k (40k agents, 7-day) 2.5s: scaled errors 377k, avg dev 17.01 user 1.88, top issues file perms 56k polling 45k FRP MTK Auth 29k DX 27k logcat 20k
- Generated simulation-report.json 27KB + simulation-feedback.md 15KB with actionable fixes, all applied

### Security
- CSP hardened, capabilities minimal, no unsafe-eval, MIT license

## [0.0.0] - Previous
- Initial scaffold with React 19 + Tauri 2.0 + 170+ FRP models, ADB integration, 2 stubs
